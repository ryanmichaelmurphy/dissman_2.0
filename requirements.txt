# Dissman pinned dependencies. Verified working on Python 3.12 (Windows dev,
# conda env "dissman") as of 2026-05-20. The Pi runs the same versions where
# wheels are available; gpiozero and python-escpos are only useful on Linux/Pi
# but they pip-install cleanly on Windows too.
#
# Install (dev or Pi):
#   python -m pip install -r requirements.txt
#
# To regenerate after a tested upgrade:
#   python -m pip freeze > requirements.lock.txt
# then promote the curated entries below.

# --- Core UI / camera / imaging ---
Kivy==2.3.1
kivy-deps.angle==0.4.0
kivy-deps.glew==0.3.1
kivy_deps.sdl2==0.8.0
Kivy-Garden==0.1.5
opencv-python==4.13.0.92
numpy==2.4.6
pillow==12.2.0

# --- TTS ---
pyttsx3==2.99

# --- OpenAI SDK and locked transitive deps ---
openai==2.37.0
httpx==0.28.1
httpcore==1.0.9
anyio==4.13.0
pydantic==2.13.4
pydantic_core==2.46.4
jiter==0.14.0
distro==1.9.0
sniffio==1.3.1
h11==0.16.0
idna==3.15
typing_extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0

# --- HTTP / env / utilities ---
requests==2.34.2
urllib3==2.7.0
certifi==2026.4.22
charset-normalizer==3.4.7
python-dotenv==1.2.2

# --- Pi hardware (no-op imports on Windows; useful on Pi) ---
gpiozero==2.0.1
colorzero==2.0
python-escpos==3.1
python-barcode==0.16.1
qrcode==8.2
PyYAML==6.0.3
six==1.17.0
appdirs==1.4.4

# --- Windows-only TTS shim ---
pywin32==311
pypiwin32==223
comtypes==1.4.16
